# Window-join
A project within the subject of Computer Programming and Programming Languages includes a stream of integers with strictly increasing timestamps that is processed. For each element, a fixed time window is considered, and the CMMDC &amp; CMMMC are determined for all pairs of values, results being displayed in timestamp order, respecting the constraints.
